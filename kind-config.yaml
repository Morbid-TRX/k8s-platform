# WHY: kind (Kubernetes IN Docker) runs a full K8s cluster inside Docker
# containers on your machine. This config defines a 1 control plane +
# 2 worker node cluster — closer to real production topology than the
# default single-node setup.
---
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4

nodes:
  - role: control-plane
    # WHY: We map ports from your localhost into the control plane node
    # so NGINX ingress can receive traffic from your browser.
    kubeadmConfigPatches:
      - |
        kind: InitConfiguration
        nodeRegistration:
          kubeletExtraArgs:
            node-labels: "ingress-ready=true"
    extraPortMappings:
      - containerPort: 80
        hostPort: 80
        protocol: TCP
        # WHY: Maps localhost:80 → NGINX inside the cluster.
        # This is how you'll access ArgoCD, Grafana etc in your browser.
      - containerPort: 443
        hostPort: 443
        protocol: TCP

  - role: worker
  - role: worker
  # WHY: 2 workers lets us test pod spreading across nodes —
  # the topologySpreadConstraints in our deployments actually work.
